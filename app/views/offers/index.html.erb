<p id="notice"><%= notice %></p>

<h3>Offers</h3>

<ul>
    <% @offers.each do |offer| %>
      <li>
        <%= link_to offer.name, offer %>
    	<%= " (expires #{offer.expires})" if offer.expires %>
      </li>
    <% end %>
</ul>
