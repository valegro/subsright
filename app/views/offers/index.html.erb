<p id="notice"><%= notice %></p>

<h3>Offers</h3>

<ul>
    <% @offers.each do |offer| %>
      <li>
        <%= link_to offer.name, offer %>
        <%= " (expires #{offer.expiry})" if offer.expiry %>
      </li>
    <% end %>
</ul>
